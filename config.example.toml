# codex_kimi_switch 配置示例。
# 复制为 %USERPROFILE%\.codex-kimi-switch\config.toml 并填入你的 Kimi API key。
# 配置文件只有这一个位置（单一事实来源）。
api_key = "sk-kimi-..."

# 可选项（默认值如下）：
# listen_addr = "127.0.0.1:8787"
# upstream_base = "https://api.kimi.com/coding/v1"
